---
description:
  zh-CN: 编辑控件：多行文本框
  en-US: SE Control Textarea
---

```html
<textarea [(ngModel)]="i.${1:remark}" name="${1}" nz-input rows="${2|1,2,3,4,5|}" placeholder="${3:必填项}"></textarea>
$0
```