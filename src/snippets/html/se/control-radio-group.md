---
description:
  zh-CN: 编辑控件：单选框组
  en-US: SE Control Radio group
---

```html
<nz-radio-group [(ngModel)]="i.${1:type}" name="${1}" ${2: required}>
  <label nz-radio [nzValue]="${3:1}">${4:类型一}</label>
  $0
</nz-radio-group>
```