---
description:
  zh-CN: 编辑控件：数字框
  en-US: SE Control Input number
---

```html
<nz-input-number [(ngModel)]="i.${1:num}" name="${1}" ${2: required}${3: nzPlaceHolder="${4}" }></nz-input-number>
$0
```